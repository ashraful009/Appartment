# ─── API Base URL ──────────────────────────────────────────────────────────────
#
# LOCAL DEVELOPMENT (this file):
#   Leave VITE_API_URL empty. Vite dev server proxies /api → http://localhost:5000
#   automatically (configured in vite.config.js). No CORS issues.
#
# PRODUCTION (set in Vercel Dashboard → Project → Settings → Environment Variables):
#   VITE_API_URL = https://nirapod-nibash.onrender.com
#   (no trailing slash)
#
VITE_API_URL=
